# Copy to .dev.vars and fill in. Use a THROWAWAY key pair here, never the
# deployed one: generate with
#   node -e "import('@mmmike/web-push').then(async m => console.log(await m.generateVapidKeys()))"
VAPID_PRIVATE_KEY=
VAPID_PUBLIC_KEY=
BACKUP_PEPPER=local-development-pepper
